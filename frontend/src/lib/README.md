# Lib

This folder contains utility functions, formatters, and wrappers for third-party libraries (e.g., an Axios instance, date-fns wrappers).

## Naming Convention

Use **kebab-case** for file names.

- Examples: `api-client.ts`, `formatters.ts`, `cn.ts`
