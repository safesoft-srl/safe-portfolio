# Store

This folder contains global state management definitions (e.g., Zustand, Redux logic, React Context definitions).

## Naming Convention

Use **camelCase** or **kebab-case** for state slice files. If it encapsulates a hook, prefix with `use`.

- Examples: `useAuthStore.ts`, `ui-store.ts`
