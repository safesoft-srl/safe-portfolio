# Features

This folder contains domain-specific feature modules. Instead of splitting a feature's code across the global `components`, `hooks`, and `services` folders, encapsulated features (like authentication or a specific dashboard section) belong here.

## Naming Convention

Use **kebab-case** for feature folder names.

- Examples: `features/auth/`, `features/user-profile/`
