# Config

This folder contains application configuration files, environment variable schemas, and central constants.

## Naming Convention

Use **camelCase** or **kebab-case** for file names.

- Examples: `appConfig.ts`, `site-settings.ts`
