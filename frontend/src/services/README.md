# Services

This folder contains the API communication logic (e.g., REST API calls). Keeping services separate ensures components just consume data without worrying about endpoints.

## Naming Convention

Use **kebab-case** for service files and **.service.ts** as extension.

- Examples: `auth.service.ts`, `user-settings.service.ts`
