# Layouts

This folder contains layout components that provide the structural shell for pages (e.g., sidebars, headers, navigation bars, footers).

## Naming Convention

Use **PascalCase** for layout files and components.

- Examples: `MainLayout.tsx`, `AuthLayout.tsx`, `DashboardLayout.tsx`
