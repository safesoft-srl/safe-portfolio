# Types

This folder contains global TypeScript type definitions, schemas (like Zod schemas), and interfaces used across multiple areas of the application.

## Naming Convention

Use **kebab-case** for type declaration files.

- Examples: `user-types.ts`, `api-responses.ts`, `index.d.ts`
