# Hooks

This folder contains reusable custom React hooks that encapsulate logic shared across multiple components.

## Naming Convention

Use **camelCase** for file names, and always start with the word `use`.

- Examples: `useAuth.ts`, `useWindowSize.ts`, `useFetchData.ts`
